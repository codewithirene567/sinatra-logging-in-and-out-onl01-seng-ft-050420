<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <h1>Welcome <%= Helpers.is_logged_in?(session).username %></h1>
    <h3>Your Balance: <%= Helpers.is_logged_in?(session).balance %></h3>
    <a href="/">Log Out</a>
  </body>
</html>
